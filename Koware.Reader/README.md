# Koware.Reader

Cross-platform reader for manga/chapters used by Koware. Provides rendering and navigation logic independent of platform-specific shells.

## Build

```bash
dotnet build Koware.Reader/Koware.Reader.csproj
```

## Notes
- Consumed by the CLI and installer to provide a bundled reader experience.
- For Windows packaging, see `Koware.Reader.Win`.
